# Saved Model Folder

After training, this folder will contain:

- `pud_xgboost_pipeline.joblib`
- `pud_xgboost_metadata.joblib`

The Django assessment form automatically uses the saved model when it exists.
